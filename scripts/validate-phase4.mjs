import fs from 'node:fs';import path from 'node:path';
const root=process.cwd();const must=['data/source_assessments/shelby_criminal_access_assessment.json','data/contracts/phase4_field_contract.json','data/governance/quarantine_reasons.json','data/judges/identity_states.json','docs/SHELBY_CRIMINAL_SOURCE_MAP.md','docs/SOURCE_ACCESS_AND_TERMS.md','docs/PUBLIC_RECORDS_REQUEST_PACKAGE.md','docs/PHASE4_DATA_READINESS_REPORT.md','scripts/phase4/import_official_extract.py','scripts/phase4/archive_source.py','scripts/phase4/validate_public_projection.py','migrations/0002_phase4_ingestion.sql'];
const missing=must.filter(f=>!fs.existsSync(path.join(root,f)));if(missing.length){console.error('Phase 4 missing:\n'+missing.join('\n'));process.exit(1)}
for(const f of ['data/sources/source_registry.json','data/source_assessments/shelby_criminal_access_assessment.json','data/contracts/phase4_field_contract.json','data/governance/quarantine_reasons.json','data/judges/identity_states.json'])JSON.parse(fs.readFileSync(path.join(root,f),'utf8'));
const reg=JSON.parse(fs.readFileSync(path.join(root,'data/sources/source_registry.json'),'utf8'));if(!reg.sources.some(s=>s.id==='shelby-criminal-prr'))throw new Error('Official records-request source missing');
const schema=fs.readFileSync(path.join(root,'src/db/schema.ts'),'utf8');for(const t of ['sourceArtifacts','ingestionRuns','recordProvenance','quarantineRecords','sentenceComponents'])if(!schema.includes(t))throw new Error(`Schema missing ${t}`);
console.log(`Phase 4 validation passed (${must.length} required artifacts, ${reg.sources.length} source records).`);
