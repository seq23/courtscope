# Add a City Operator Guide

A city moves through DRAFT, source upload, mapping, dry run, validation, and activation. Activation is automatic only after every hard gate passes.

Never force activation when sources are unverifiable, case identifiers are unusable, court types cannot be separated, required variables are missing, privacy is unsafe, scoring is invalid, or identity conflicts remain.

